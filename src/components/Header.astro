---
import { isEnabled } from '../config/helpers';

const nav = [
  { label: 'Home', href: '/' },
  { label: 'Blog', href: '/blog' },
  ...(isEnabled('products') ? [{ label: 'Products', href: '/products' }] : []),
];

const currentPath = Astro.url.pathname;
const currentLocale = Astro.currentLocale || 'id';

// Build language switch URLs
function getLocalePath(locale: string) {
  const path = currentPath.replace(/^\/(en|id)/, '') || '/';
  if (locale === 'id') return path;
  return `/${locale}${path === '/' ? '' : path}`;
}
---

<header class="sticky top-0 z-50 bg-surface/80 backdrop-blur-lg border-b border-surface-100">
  <nav class="container mx-auto px-4 h-16 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="text-xl font-bold hover:text-accent transition-colors">
      Narzo<span class="text-accent">.</span>Store
    </a>
    
    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center gap-6">
      {nav.map(item => (
        <a 
          href={item.href} 
          class={`hover:text-accent transition-colors ${
            currentPath === item.href ? 'text-accent' : ''
          }`}
        >
          {item.label}
        </a>
      ))}
      
      <!-- Language Switcher -->
      <div class="flex items-center gap-1 text-sm border border-surface-100 rounded-lg overflow-hidden">
        <a 
          href={getLocalePath('id')}
          class={`px-2 py-1 transition-colors ${currentLocale === 'id' ? 'bg-accent text-white' : 'hover:bg-surface-50'}`}
        >
          ID
        </a>
        <a 
          href={getLocalePath('en')}
          class={`px-2 py-1 transition-colors ${currentLocale === 'en' ? 'bg-accent text-white' : 'hover:bg-surface-50'}`}
        >
          EN
        </a>
      </div>
      
      <!-- Search icon -->
      <a href="/search" class="btn-icon" aria-label="Search">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
        </svg>
      </a>
    </div>
    
    <!-- Mobile Menu Button -->
    <button 
      id="mobile-menu-btn" 
      class="md:hidden btn-icon"
      aria-label="Toggle menu"
      aria-expanded="false"
    >
      <svg class="w-6 h-6 menu-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
      </svg>
      <svg class="w-6 h-6 close-icon hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </button>
  </nav>
  
  <!-- Mobile Navigation -->
  <div id="mobile-menu" class="hidden md:hidden border-t border-surface-100">
    <div class="container mx-auto px-4 py-4 flex flex-col gap-2">
      {nav.map(item => (
        <a 
          href={item.href} 
          class={`py-2 px-4 rounded-lg hover:bg-surface-50 transition-colors ${
            currentPath === item.href ? 'text-accent bg-surface-50' : ''
          }`}
        >
          {item.label}
        </a>
      ))}
      
      <!-- Mobile Language Switcher -->
      <div class="flex items-center gap-2 py-2 px-4">
        <span class="text-gray-500 text-sm">Language:</span>
        <a 
          href={getLocalePath('id')}
          class={`px-3 py-1 rounded text-sm ${currentLocale === 'id' ? 'bg-accent text-white' : 'bg-surface-50'}`}
        >
          ID
        </a>
        <a 
          href={getLocalePath('en')}
          class={`px-3 py-1 rounded text-sm ${currentLocale === 'en' ? 'bg-accent text-white' : 'bg-surface-50'}`}
        >
          EN
        </a>
      </div>
      
      <!-- Mobile Search -->
      <a href="/search" class="py-2 px-4 rounded-lg hover:bg-surface-50 flex items-center gap-2">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
        </svg>
        Search
      </a>
    </div>
  </div>
</header>

<script>
  const menuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = menuBtn?.querySelector('.menu-icon');
  const closeIcon = menuBtn?.querySelector('.close-icon');
  
  menuBtn?.addEventListener('click', () => {
    const isOpen = mobileMenu?.classList.toggle('hidden');
    menuIcon?.classList.toggle('hidden');
    closeIcon?.classList.toggle('hidden');
    menuBtn.setAttribute('aria-expanded', String(!isOpen));
  });
</script>
